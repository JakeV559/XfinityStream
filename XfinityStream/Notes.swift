/*
 
 struct item: Identifiable {
    var id: Int
 }
 
 ForEach(input, id: \.id) { name in
     ImageTemplate(name: name.pFront)
 }
 
 Can split the nav view and nav link across views
 
 .navigationBarTitle("")
 .navigationBarHidden(true)
 Inside of navigationo view to hide the space
 
 How to use the color extension: Color(0x292b2c)
 
 ImageTemplate: .resizeable() .frame(width: 300, height: 400)
 
 TextTemplate:  .font(.system(size: 30))
 
 ZStack(alignment: .topleading)
 
 VStack(alignment: .leading)
 
 HStack {
 
 }
    .padding(.top, 20)
    .padding(.bottom, 40)
 
 */
